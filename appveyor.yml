version: 1.0.{build}

image: Visual Studio 2015

clone_folder: c:\projects\open-samp-api

platform: Win32
configuration: Release

install:
    - nuget restore src\Open-SAMP-API.sln

build:
    - "C:\Program Files (x86)\MSBuild\14.0\Bin\MSBuild" src\Open-SAMP-API.sln /p:Configuration=Release /p:Platform=Win32
    
